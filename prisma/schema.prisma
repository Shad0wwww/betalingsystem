generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model User {
    id           String @id @default(uuid())
    name         String
    email        String @unique
    phone        String @unique
    phoneCountry String @default("DK")
    role         Role   @default(USER)

    reservedBalance Float @default(0)

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    stripeCustomerId String? @unique

    //Relationer
    verificationTokens VerificationToken[]
    invoices           Invoice[]
    boats              Boat[]
    transactions       Transaction[]
    auditLogs          AuditLog[]
    meterSessions      MeterSession[]
}

model AuditLog {
    id        String     @id @default(cuid())
    userId    String
    user      User       @relation(fields: [userId], references: [id])
    action    ActionType
    details   String?
    timestamp DateTime   @default(now())
}

model VerificationToken {
    identifier String   @unique
    token      String   @unique
    expires    DateTime

    userId String
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([identifier, token])
}

model Invoice {
    id     Int    @id @default(autoincrement())
    userId String
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    InvoiceNumber String? @unique

    meterSessionId Int?
    meterSession   MeterSession? @relation(fields: [meterSessionId], references: [id])

    amount   Int
    currency String        @default("dkk")
    status   InvoiceStatus @default(PENDING)

    type UtilityType

    stripeSessionId       String? @unique
    stripePaymentIntentId String? @unique

    dueDate   DateTime
    paidAt    DateTime?
    createdAt DateTime  @default(now())
}

model Boat {
    id     Int    @id @default(autoincrement())
    userId String
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    kaldeNavn String
    skibModel String

    meterSessions MeterSession[]
}

model MeterSession {
    id Int @id @default(autoincrement())

    // Brug meterId (intern database ID) til relationen, ikke Modbus deviceID
    meterId Int
    meter   Meter @relation(fields: [meterId], references: [id])

    userId String
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    boatId Int
    boat   Boat @relation(fields: [boatId], references: [id], onDelete: Cascade)

    type UtilityType

    startValue Float
    endValue   Float?

    startTime DateTime  @default(now())
    endTime   DateTime?
    isActive  Boolean   @default(true)

    readings MeterReading[]
    invoice  Invoice[]
}

model MeterReading {
    id Int @id @default(autoincrement())

    meterId Int
    meter   Meter @relation(fields: [meterId], references: [id])

    type  UtilityType
    value Float
    date  DateTime    @default(now())

    sessionId Int?
    session   MeterSession? @relation(fields: [sessionId], references: [id])

    @@index([meterId, date])
}

model Meter {
    id       Int         @id @default(autoincrement())
    deviceId String         @unique // Modbus ID (Kan nu ændres uden at smadre historik!)
    type     UtilityType
    location String?
    status   MeterStatus @default(OFFLINE)

    sessions MeterSession[]
    readings MeterReading[]
}

model Transaction {
    id     Int    @id @default(autoincrement())
    userId String
    user   User   @relation(fields: [userId], references: [id])

    amount Float           @default(0)
    type   TransactionType

    stripeSessionId String? @unique

    createdAt DateTime @default(now())
}

enum TransactionType {
    PAID
    REFUND
    RESERVED
    RESERVATION_RELEASED
}

enum InvoiceStatus {
    PENDING
    PAID
    FAILED
    OVERDUE
}

enum UtilityType {
    WATER
    ELECTRICITY
}

enum Role {
    USER
    ADMIN
}

enum ActionType {
    // Auth & Brugerstyring
    ACCOUNT_CREATED
    ACCOUNT_DELETED
    LOGIN
    LOGOUT
    LOGIN_OTP_SENT
    LOGIN_OTP_SENT_SUCCESS
    EMAIL_SEND_OTP_SUCCESS
    EMAIL_SEND_OTP_FAILED
    
    // Brugerændringer
    EMAIL_CHANGE
    NAME_CHANGE
    PHONE_CHANGE
    ROLE_CHANGED
    
    // Økonomi & Betaling
    LINKED_STRIPE
    PAYMENT_LINK_CREATED
    PAYMENT_MADE
    PAYMENT_FAILED
    REFUND_PROCESSED
    RESERVED_BALANCE
    INVOICE_CREATED
    INVOICE_CANCELED
    
    // Både
    BOAT_ADDED
    BOAT_REMOVED
    
    // Måler-administration (Hardware)
    CREATE_METER
    UPDATE_METER
    DELETE_METER
    CONNECTED_METER
    DISCONNECTED_METER
    
    // Måler-sessioner (Forbrug)
    SESSION_STARTED
    SESSION_ENDED
    SESSION_FORCE_STOPPED
    SESSION_AUTO_STOPPED
    
    // Admin handlinger
    ADMIN_ACTION
}

enum MeterStatus {
    ONLINE
    OFFLINE
    MAINTENANCE
}